<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners android:radius="@dimen/select_item_width"/>

    <solid
            android:color="@color/select_bg"/>

    <size
            android:height="@dimen/select_item_width"
            android:width="@dimen/select_item_width"/>

    <stroke
            android:width="@dimen/select_border_width"
            android:color="@color/select_default"/>

</shape>